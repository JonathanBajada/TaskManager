// Import the existing LoginPage component
// TODO: Consider moving LoginPage logic directly here if it's page-specific
import LoginPageComponent from '../components/LoginPage';

const LoginPage = () => {
	return <LoginPageComponent />;
};

export default LoginPage;

